export default function getConfig(key: string): string {
  // get it from firebase
  // get it from otp config
  // get it from any other source
}

// export function getOrderConfigs();

// exposing keys to get the values or just the functions

// one way to build this would be to create the list of all the named functions maybe combine
// feature configs into single one

// sanitise the configs for easy access
